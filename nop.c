#include "monty.h"
/**
 * f_nop- shows nothing
 * @head: shows stack head
 * @counter: shows the line_number
 * Return: nope
 */
void f_nop(stack_t **head, unsigned int counter)
{
	(void) counter;
	(void) head;
}
