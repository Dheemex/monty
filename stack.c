#include "monty.h"
/**
 * f_stack - it prints the top
 * @head: shows the stack head
 * @counter: shows the line_numb
 * Return: no return
 */
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
